<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cardproject.myapp.model.community">
	
	<!-- test -->
	<select id="selectTest" resultType="communityvo">
		SELECT * FROM COMMUNITY WHERE COMM_ID=1
	</select>
	
</mapper>

